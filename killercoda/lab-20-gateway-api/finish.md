# Well done!

You have completed Lab 20 — Gateway API:

✅ Installed the Gateway API standard CRDs
✅ Deployed NGINX Gateway Fabric as the GatewayClass controller
✅ Created a Gateway object declaring an HTTP listener on port 80
✅ Created an HTTPRoute that matched by hostname and forwarded to the echo Service
✅ Tested routing with curl using the Host header
✅ Compared the Gateway API role model (GatewayClass / Gateway / HTTPRoute) with legacy Ingress

**Next:** Lab 21 — Network Policies
