# Well done!

You have completed Lab 19 — Ingress Controller and Resources:

✅ Installed ingress-nginx controller via bare-metal NodePort manifest
✅ Deployed two echo backends and exposed them as Services
✅ Created an Ingress resource with two host-based routing rules
✅ Tested host-based routing using curl with a Host header
✅ Added TLS termination via a self-signed certificate stored as a Secret
✅ Reviewed path-based routing as an alternative pattern

**Next:** Lab 20 — Gateway API
