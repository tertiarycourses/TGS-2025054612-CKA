# Well done!

You have completed Lab 17 — Pod-to-Pod Connectivity:

✅ Launched a netshoot client and nginx server pod on the flat pod network
✅ Verified pod-to-pod ping and curl using raw pod IPs (no NAT)
✅ Exposed the server pod as a Service and resolved it by DNS name
✅ Inspected the pod's network interface, routing table, and resolv.conf
✅ Traced the hop count between pods across nodes

**Next:** Lab 18 — Service Types: ClusterIP, NodePort, LoadBalancer
