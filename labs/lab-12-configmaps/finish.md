# Well done!

You have completed Lab 12 — ConfigMaps:

✅ Created a ConfigMap from literal key/value pairs
✅ Created a ConfigMap from a file using `--from-file`
✅ Consumed a ConfigMap as environment variables via `envFrom`
✅ Consumed a ConfigMap as a mounted volume file
✅ Observed that mounted ConfigMaps update in-place while env vars require a pod restart
✅ Cleaned up all pods and ConfigMaps

**Next:** Lab 13 — Secrets
