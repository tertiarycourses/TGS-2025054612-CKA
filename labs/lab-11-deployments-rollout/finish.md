# Well done!

You have completed Lab 11 — Deployments: Rolling Update and Rollback:

✅ Created a Deployment with 4 replicas and verified rollout status
✅ Inspected the default RollingUpdate strategy (maxSurge, maxUnavailable)
✅ Performed a rolling image update and watched pods cycle
✅ Triggered a bad rollout with a non-existent image and observed ImagePullBackOff
✅ Rolled back to a previous revision using `kubectl rollout undo`
✅ Paused a rollout, batched multiple changes, then resumed as a single rollout
✅ Cleaned up the Deployment

**Next:** Lab 12 — ConfigMaps
