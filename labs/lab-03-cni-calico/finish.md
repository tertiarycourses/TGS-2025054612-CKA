# Well done!

You have completed Lab 3 — Install a CNI Plugin (Calico):

✅ Applied the Calico manifest to install the CNI DaemonSet and controllers
✅ Watched `calico-node` pods come up and nodes transition to `Ready`
✅ Verified cross-node pod connectivity with a ping test
✅ Inspected CNI configuration in `/etc/cni/net.d/` and binaries in `/opt/cni/bin/`
✅ Cleaned up the test pods

**Next:** Lab 4 — Cluster Upgrade with kubeadm
