# Well done!

You have completed Lab 25 — Volume Types in Pods:

✅ Used emptyDir to share scratch space between two containers in the same pod
✅ Mounted a node directory using hostPath and observed the security trade-off
✅ Projected ConfigMap and Secret data as files inside a container
✅ Combined multiple sources into one mount point with a projected volume
✅ Exposed pod labels and name to the container via downwardAPI
✅ Cleaned up all pods and associated resources

**Next:** Lab 26 — Troubleshoot Cluster Components
