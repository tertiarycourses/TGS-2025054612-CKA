# Well done!

You have completed Lab 6 — Install Components with Helm:

✅ Installed the Helm CLI and added the Bitnami chart repository
✅ Deployed nginx with `helm install` using `--set` overrides
✅ Upgraded the release by applying a `values.yaml` file
✅ Rolled back to revision 1 with `helm rollback`
✅ Rendered manifests without installing using `helm template`
✅ Uninstalled the release and deleted the namespace

**Next:** Lab 7 — Customize Manifests with Kustomize
