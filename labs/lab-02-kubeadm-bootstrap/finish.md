# Well done!

You have completed Lab 2 — Bootstrap a Cluster with kubeadm:

✅ Ran `kubeadm init` on the control plane with a pod CIDR and advertise address
✅ Configured `kubectl` by copying the admin kubeconfig into `~/.kube/config`
✅ Joined the worker node using the bootstrap token
✅ Inspected the running kube-system pods and node status
✅ Explored the PKI, static manifests, and kubeconfigs that kubeadm created

**Next:** Lab 3 — Install a CNI Plugin (Calico)
